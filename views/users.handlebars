<div class="container">
   <section class="row justify-content-center">
      {{#each users as |user|}}
      <div class="text-center" id="namestyle">

         <h3>👨‍🌾{{user.name}}</h3>
         <br>

      </div>
      {{/each}}

   </section>

</div>